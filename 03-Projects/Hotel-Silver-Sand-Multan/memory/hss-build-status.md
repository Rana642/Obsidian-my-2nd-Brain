---
name: hss-build-status
description: Hotel Silver Sand website rebuild — phase plan and current progress
metadata: 
  node_type: memory
  type: project
  originSessionId: 48f648cb-9c79-4134-90c6-bf52b9121542
---

Hotel Silver Sand Multan website rebuild (Next.js SSR replacing indexing-broken React CSR site). Client provided 4 planning docs (build brief, content, SEO/AEO/GEO plan, strategy) as chat attachments — NOT on disk; key facts are captured in `src/lib/constants.ts` and README.md.

**Phase plan (pause after each for client testing):**
1. ✅ Setup — Next.js 16.2 (App Router, TS, Turbopack) + Tailwind v4 + Supabase schema (done 2026-07-03, client tested OK)
2. ✅ Public site — COMPLETE (2026-07-04, all 28 routes build static/SSG, all verified 200). Homepage (src/components/home/), shared layout (Header/Footer/MobileStickyBar/Logo in src/components/layout/), /rooms + 4 detail pages ([slug] with generateStaticParams), about, contact (ContactForm→contacts table), facilities, gallery (empty-state until photos), discover-multan, FAQ (12 Q&A in src/lib/faq.ts via native <details>), /book-direct (BookingForm→bookings table, ?room= preselect, dynamic route), 6 SEO landing pages via LocalLandingPage template (src/components/landing/), blog list+[slug] (paragraph renderer; markdown lib deferred to Phase 5 editor), thank-you (noindex), privacy/terms, custom 404. Data layer src/lib/data/* falls back to FALLBACK_ROOMS/places mirrors when Supabase env missing. Forms use useActionState + server actions (src/lib/actions.ts, anon insert via RLS) and already push exact GTM events via src/lib/gtm.ts: begin_checkout (form focus), booking_confirmed (submit→/thank-you), contact_form_submit. hero.jpg is Unsplash stock PLACEHOLDER — client must supply real hotel photos (hero + rooms + gallery). Sitemap lists all 22 public URLs.
3. ✅ SEO/AEO/GEO — COMPLETE (2026-07-04, verified in served HTML). robots.ts (exact brief spec, AI crawlers), public/llms.txt, sitemap.ts (all 22 public URLs; blog post URLs still TODO when posts exist). All 8 schema types via src/lib/schema.ts builders + src/components/seo/JsonLd.tsx: Organization (layout), Hotel+LodgingBusiness w/ AggregateRating+Reviews+containsPlace rooms (@id {url}/#hotel — home, about, 6 landing pages), FAQPage+Speakable (faq, .faq-item selector; home uses .hotel-summary/.key-facts/.location-info), HotelRoom (room details, king has BedDetails), LocalBusiness (contact, 24/7 hours), BreadcrumbList (all pages), Offer (book-direct), BlogPosting+ItemList extras. starRating 2 included per client content doc. Canonicals on every page via alternates. OG defaults in layout + app/opengraph-image.tsx (navy/gold 1200×630 via next/og). NOTE: schema counts appear doubled in raw HTML grep (RSC flight payload duplication — actual script tags are half; normal).
4. ✅ GTM tracking — COMPLETE (2026-07-04, verified: container in HTML on all pages, GA4/Pixel confirmed NOT hardcoded). src/components/tracking/: GoogleTagManager.tsx (GtmScript afterInteractive + GtmNoScript first in body), FunnelTracker.tsx (global capture-phase click listener → whatsapp_click/call_click/bookingcom_click/email_click/directions_click with link_url/link_text/page_path), TrackEvent.tsx (mount-once → view_item on room pages w/ item_name+price, view_item_list on /rooms). Form events wired since Phase 2 (begin_checkout on focus, booking_confirmed conversion, contact_form_submit). Client must verify in GTM Preview (Tag Assistant → localhost:3000) before launch.
5. ✅ Admin — COMPLETE (2026-07-04, build 45 routes, guards verified: all /admin/* 307→/admin/login logged out, export routes 401). Supabase Auth: src/proxy.ts (Next 16 proxy, session refresh, matcher /admin/*), login at /admin/login (client signInWithPassword → full nav), guarded shell src/app/admin/(dashboard)/layout.tsx via requireAdmin/requireModule (src/lib/admin/auth.ts; canAccess in permissions.ts — importable client-side). Roles: super_admin all / admin all-but-users / staff dashboard+bookings+contacts. All 9 modules under src/app/admin/(dashboard)/: dashboard (stats+recent), bookings (filters/search, StatusSelect, CSV export route, calendar month view), contacts (+CSV), rooms (list+VisibleToggle+RoomForm edit/new, ImageUploader→hotel-images bucket), gallery, places, blog (PostForm, auto-slug, published_at), users (super_admin only; create via service-role auth.admin+admin_users row, role change, delete cascades), funnels (bookings by source/status bars, 30-day stats, GA4 link), settings (site_settings editor). Server actions in src/lib/admin/actions.ts (session re-verified; service client only for user mgmt after super_admin check).
   DB LIVE (2026-07-04): project jwzjlvagofqmwflfkkxc.supabase.co, schema 001 run by client, seed loaded remotely by me via REST (4 rooms/7 settings/6 places). .env.local uses LEGACY JWT keys — new sb_publishable_/sb_secret_ keys returned 401 (Supabase incident that day); consider switching later. Super admin auth user created via API: shoaib.nabi.noor@gmail.com, temp password issued in chat 2026-07-04 — client must change it (Supabase dashboard → Auth → user → or add password-change UI later).
6. ⬜ Test & launch — Vercel, domain switch, Bing+Google sitemap submission

**Post-Phase-5 client changes (done 2026-07-06, verified via Playwright screenshots at 375/768/1440):**
- Hero redesigned: min-h-svh video hero (video layer plays public/videos/hero.mp4 when client uploads it — README in that folder; Image fallback + poster until then), reorganized content (eyebrow/name/tagline "40 Years of Trusted Comfort"/info line w/ 822+ reviews), AvailabilityBar widget (src/components/home/AvailabilityBar.tsx — fires begin_checkout, pushes to /rooms?checkin&checkout&adults&children), 3-tier CTAs (WhatsApp gold / Call outline / Booking.com subtle)
- Search flow: RoomCardActions (client, useSearchParams+Suspense — shows "Book This Room" carrying dates when search params present), AvailabilityNotice banner on /rooms, book-direct + BookingForm accept defaultCheckIn/defaultCheckOut/defaultGuests
- About snapshot: split layout (about.jpg placeholder left w/ gold corner accents + Since 1986 badge, story right, stacked mobile), button renamed "Discover Our Story", At-a-Glance speakable card kept below
- Name consistency: 16 fixes — full "Hotel Silver Sand Multan" everywhere (exceptions: logo lockup, verbatim guest quotes)
- Touch targets ≥44px (hamburger, footer socials, all CTAs). Playwright installed as devDependency (chromium) for screenshot verification — reusable in Phase 6.
- Client must upload: public/videos/hero.mp4 + real photos replacing hero.jpg and about.jpg stock placeholders.

**2026-07-08 changes:** Old-site style match (see hss-working-style design language) applied via bulk script across 62 files. Offer pricing feature: rooms.offer_price column (migration 004_room_offer_price.sql — CLIENT MUST RUN in SQL Editor; until then admin room-save errors, public site fine), hasOffer()/effectivePrice() in src/lib/data/rooms.ts, strikethrough+SAVE% badge on RoomCard + room detail + schema offers price, admin RoomForm "Offer Price" field w/ validation offer<price. Artifact review page redeployed (same URL).

**Key implementation decisions:**
- Tailwind v4 with `--color-*: initial` in globals.css — default palette wiped, only 8 brand tokens exist (navy/royal/gold/gold-rich/white/offwhite/gray-soft/gray-dark + black for shadows)
- Fonts: Playfair Display (font-serif) + Inter (font-sans) via next/font
- DB: supabase/migrations/001_initial_schema.sql (8 tables, RLS with security-definer is_admin()/is_super_admin(), hotel-images storage bucket) + 002_seed.sql (4 rooms exact prices, site_settings, 6 multan_places). Client must run these in Supabase SQL Editor + fill .env.local
- Old live site stays running until launch; this is a fresh build in `g:\Hotel SS\Hotel Silver Sand Website`
- Gotcha: running `next dev` after `next build` on this machine serves 404s — delete `.next` before restarting the dev server after any production build

See [[hss-non-negotiables]] for locked constraints.
